#!/usr/bin/env bash
###############################################################################
CMD_SCRIPT=$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)
###############################################################################

source "$CMD_SCRIPT/../.env" ;
cast send --rpc-url main --private-key $PRIVATE_KEY \
    $MAIN_CALCULATOR "subAt(address,uint256,uint256)" \
    $APOW_CALCULATOR "$1" "$2" ;

###############################################################################
###############################################################################
